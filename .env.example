# The committed config/models.json uses these env-var references instead of raw API keys.
# Put real values in your shell profile, password manager, or an uncommitted .env file.
AZURE_FOUNDRY_TECH_API_KEY=
COSINE_API_KEY=
